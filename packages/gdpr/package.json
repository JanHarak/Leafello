{
  "name": "@dietapp/gdpr",
  "version": "0.1.0",
  "private": true,
  "description": "Čistá logika pro GDPR: co exportovat a v jakém pořadí mazat data uživatele (N-06, smazání účtu).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
