{
  "name": "@dietapp/reminders",
  "version": "0.1.0",
  "private": true,
  "description": "Generátor rozvrhu připomínek: fázované pití a jídlo (F-13, max 8 notifikací denně).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  }
}
